# Аудит безопасности

Документ описывает принятые меры защиты, результаты проверок и осознанно
принятые риски. Обновляется на каждом этапе.

**Целевая среда:** VPS 1 vCPU / 1 GB RAM, доступ по IP-адресу, без домена

| Этап | Объём | Дата |
|---|---|---|
| 0 | nginx + Django | 2026-09-17 |
| 1 | PostgreSQL, пользователи, роли, шифрование ПДн | 2026-09-17 |

---

# Этап 1 — база данных и учётные записи

## Как проверялось

Контейнеры собрать не удалось: политика сетевого доступа этой среды
блокирует реестр образов Docker. Поэтому проверка велась иначе — поднимался
**настоящий PostgreSQL 16** с теми же сгенерированными `pg_hba.conf` и
сертификатом, что уедут в контейнер, и против него выполнялись миграции,
сценарии регистрации и входа.

Что это покрыло: схему, ограничения целостности, разделение прав ролей,
шифрование, TLS-канал, миграции на чистой базе.

**Что осталось непроверенным: запуск самого контейнера `db`.** Он описан с
`read_only`, `cap_drop: ALL` и запуском от `999:999`. Если контейнер не
поднимется при первой инициализации, первое, что стоит временно снять, —
`read_only: true` у сервиса `db`.

## Результаты проверок

| Инструмент | Результат |
|---|---|
| `django check --deploy` | замечаний нет, кроме связанных с выключенным TLS сайта |
| `makemigrations --check` | схема и миграции синхронны |
| `bandit` | 0 срабатываний |
| `pip-audit` | 0 уязвимостей |
| миграции на чистой базе | применяются полностью |

### Дефекты, найденные и устранённые в ходе аудита

1. **Миграция ломала развёртывание на чистой базе.** Миграция
   `accounts.0002` изменяла таблицу приложения `notifications`, не объявляя
   зависимости от него. Проверка на пустой базе показала, что Django
   применяет `accounts.0002` **раньше** `notifications.0001` — на новом
   сервере развёртывание упало бы на первом же запуске. Разделено на две
   миграции, каждая в своём приложении.

2. **Уязвимая версия библиотеки шифрования.** `pip-audit` нашёл 13
   записей об уязвимостях в `cryptography 46.0.3`. Обновлено до 50.0.1,
   совместимость проверена на данных, зашифрованных прежней версией.

3. **Значения по умолчанию отсутствовали в схеме.** Django выставляет
   `default` только в Python, поэтому в самой таблице `role` не имела
   `DEFAULT`. Запись, сделанная в обход приложения, не получила бы роль
   покупателя. Добавлена миграция с `ALTER COLUMN ... SET DEFAULT`.

## Что проверено на работающей базе

| Сценарий | Результат |
|---|---|
| Адрес почты в дампе базы открытым текстом | не найден ни разу |
| Токен из письма в теле очереди открытым текстом | не найден |
| Тело сообщения после отправки | стирается |
| Роль приложения: `DROP TABLE` | отказ (не владелец) |
| Роль приложения: `CREATE TABLE`, `CREATE FUNCTION` | отказ (нет прав на схему) |
| Роль приложения: чтение `pg_authid` | отказ |
| Роль приложения: `COPY TO FILE`, `pg_read_file` | отказ |
| Роль приложения: `CREATE DATABASE` | отказ |
| Роль бэкапа: `INSERT` | отказ (read-only транзакция) |
| Канал до БД | TLSv1.3, `TLS_AES_256_GCM_SHA384` |
| Подключение с чужим корневым сертификатом | отбито |
| Логин `ivanpetrov` при существующем `IvanPetrov` | отклонён |
| Повторная регистрация на занятый адрес | отклонена |
| Логины `admin`, `ADMIN`, `adm1n`, `4dmin`, `@dmin` | отклонены |
| Кириллический логин | отклонён |
| Вход до подтверждения почты | запрещён |
| Вход по логину и по адресу почты после подтверждения | работает |
| Время ответа: существующий и несуществующий логин | отличаются на 1% |
| Второй основной адрес у пользователя | отклонён |
| Несуществующая роль или статус | отклонены |
| Обезличивание | адреса удалены, строка заказа сохранена, вход невозможен |
| Просроченная регистрация | адрес освобождён |
| Хеш документа в записи о согласии | сходится с текстом |

## Устройство защиты данных

### Шифрование персональных данных

Адреса почты хранятся только зашифрованными: AES-256-GCM, шифрование на
стороне приложения, база открытого текста не видит никогда.

Поиск при входе идёт по **слепому индексу** — `HMAC-SHA256(перец, адрес)`.
Зашифрованный столбец искать нельзя: AES-GCM даёт разный шифротекст при
каждом вызове. HMAC же от одного значения всегда одинаков.

Три ключа разделены намеренно: ключ шифрования, перец слепого индекса и
перец для токенов. Компрометация перца для поиска позволяет лишь
проверить, есть ли в базе известный заранее адрес, но не расшифровать ни
одной записи.

Связанные данные (AAD) привязывают шифротекст к конкретному столбцу:
перенести значение из одной таблицы в другую не получится, расшифровка
провалится.

**От чего это защищает:** утечка дампа или бэкапа. Украденная база
превращается из готового списка контактов в набор бесполезных блобов.

**От чего не защищает:** выполнение кода в контейнере приложения. Ключ
лежит там же. Приукрашивать здесь нечего.

**Чем за это заплачено:** поиск по почте только по точному совпадению.
Найти в админке пользователя по части адреса нельзя.

### Разделение прав в базе данных

| Роль | Права | Кто использует |
|---|---|---|
| `selectashop_owner` | владелец схемы, DDL | только миграции |
| `selectashop_app` | SELECT/INSERT/UPDATE/DELETE | приложение |
| `selectashop_backup` | только чтение | резервное копирование |
| `postgres` | суперпользователь | ручное обслуживание |

Django подключается двумя разными ролями: обычные запросы идут через
`default` (роль приложения), миграции — через алиас `admin` (владелец
схемы). Проверено: под ролью приложения не выполняется ни `DROP TABLE`,
ни `CREATE FUNCTION`, ни чтение системных таблиц с хешами паролей.

Роли приложения выставлены `statement_timeout = 15s` и
`idle_in_transaction_session_timeout = 30s`. Владельцу схемы таймаут
снят: создание индекса на большой таблице законно длится дольше.

### Изоляция базы данных

```
edge:     nginx <-> интернет
internal: nginx <-> web          (internal: true)
data:     web   <-> db           (internal: true)
```

Отдельная сеть `data` заведена ради одного свойства: **у nginx нет доступа
к базе данных**. Порт 5432 не публикуется на хосте вовсе.

Контейнер БД: `read_only`, `cap_drop: ALL`, `no-new-privileges`, запуск
сразу от пользователя `999:999`. Последнее обязательно — при сброшенных
capabilities процесс уже не смог бы понизить себе права с root, так что
запускаться от root нельзя.

### Учётные записи

| Мера | Назначение |
|---|---|
| роль по умолчанию `buyer` в схеме и в CHECK-ограничении | повысить себе права записью мимо приложения нельзя |
| роль не является полем ни одной формы | менять её можно только командой из терминала |
| Argon2id, минимум 10 символов | рекомендация OWASP против перебора на видеокартах |
| выравнивание времени ответа при входе | нельзя перебрать список существующих логинов |
| регистронезависимая уникальность логина | исключает пару `Support` и `support` |
| только латиница в логинах | кириллическая «а» неотличима от латинской «a» |
| свёртка похожих символов для служебных имён | блокирует `adm1n`, `4dmin`, `@dmin` |
| срок жизни неподтверждённой регистрации | нельзя заблокировать чужой адрес, заняв его |
| хеши вместо самих токенов | утёкший дамп не позволит сбросить чужой пароль |
| шифрование и стирание тела сообщения очереди | токен не переживает отправку |

---

# Этап 0 — nginx и Django

**Дата аудита:** 2026-09-17
**Объём:** nginx + Django, без базы данных, без аутентификации

---

## 1. Результаты автоматических проверок

| Инструмент | Что проверяет | Результат |
|---|---|---|
| `django check --deploy` | настройки развёртывания Django | замечаний нет, кроме связанных с выключенным TLS |
| `bandit` | статический анализ Python на уязвимые конструкции | 0 срабатываний |
| `pip-audit` | известные CVE в зависимостях | 0 уязвимостей |
| `gixy` | анализ конфигурации nginx | 0 проблем |
| `nginx -t` | синтаксис конфигурации | успешно |

Отдельно отмечу: проверка `gixy` на `alias_traversal` не сработала. Это
подтверждает, что директивы `alias` в блоках `/static/` и `/media/`
построены корректно и не позволяют выйти за пределы каталога.

Повторить всё разом: `make audit`.

### Дефекты, найденные и устранённые в ходе аудита

1. **Дублирование заголовков безопасности.** Заголовки выставляли
   одновременно nginx и Django, клиент получал по два экземпляра каждого.
   Для `X-Frame-Options` это не косметика: при нескольких экземплярах
   часть браузеров считает заголовок некорректным и перестаёт применять
   защиту от фрейминга. Устранено: копии от приложения снимаются через
   `proxy_hide_header`, единственным источником остаётся nginx. Настройки
   Django намеренно оставлены включёнными — если приложение когда-нибудь
   окажется доступно в обход nginx, оно защитит себя само.

2. **Утечка манифеста статики.** Файл `staticfiles.json` отдавался наружу
   и раскрывал полный перечень файлов проекта. Закрыт.

3. **Отсутствие `worker_rlimit_nofile`.** При `worker_connections 512`
   лимит дескрипторов оставался системным. Под нагрузкой это приводит к
   отказам `EMFILE`, поскольку на каждое клиентское соединение может
   приходиться второе — к приложению. Выставлено 2048, согласовано с
   `ulimits.nofile` в `docker-compose.yml`.

4. **Отсутствие CSRF-middleware.** Было исключено вместе с остальными
   подсистемами, зависящими от БД. Однако защита от CSRF работает на
   cookie и к базе не обращается — включена постоянно.

---

## 2. Проверенное поведение

Проверки выполнены на работающей связке nginx + gunicorn.

| Сценарий | Ожидание | Факт |
|---|---|---|
| Главная страница | 200 | 200 |
| Запрос с чужим `Host` | соединение закрыто | обрыв без ответа (444) |
| Метод `DELETE` | 405 | 405 |
| `POST` без CSRF-токена | 403 | 403 |
| `/.env`, `/.git/config`, `/backup.sql` | 404 | 404 |
| `/static/staticfiles.json` | 404 | 404 |
| `/media/evil.svg`, `/media/evil.html` | 404 | 404 |
| `/media/ok.gif` | 200 | 200 |
| Выход за пределы каталога (`../`, `%2e%2e`, `..%2f`) | отказ | 400 |
| Тело запроса 12 МБ при лимите 8 МБ | 413 | 413 |
| 120 запросов подряд при лимите 20 r/s | часть отклонена | 66 из 120 отклонено с кодом 429 |
| Заголовки безопасности на статике | присутствуют | присутствуют |
| Дублирующиеся заголовки | отсутствуют | отсутствуют |

Потребление памяти на холостом ходу: nginx около 9 МБ, gunicorn около
110 МБ на три процесса. Лимиты контейнеров — 64 МБ и 320 МБ.

---

## 3. Меры защиты по уровням

### Изоляция контейнеров

| Мера | Назначение |
|---|---|
| `read_only: true` | корневая ФС недоступна на запись: полезную нагрузку после RCE некуда положить |
| `cap_drop: ALL` | ни одной linux-capability; оба сервиса работают от непривилегированных пользователей на портах выше 1024 |
| `no-new-privileges:true` | процесс не может повысить права, в том числе через setuid-бинарники |
| сеть `internal: true` | контейнер приложения не может инициировать соединение в интернет: ни выгрузить данные, ни скачать вторую стадию нагрузки |
| порты приложения не публикуются | Django достижим только через nginx |
| `mem_limit`, `cpus`, `pids_limit` | отказ локализуется в одном контейнере, а не выбирается OOM-killer случайно |
| `tmpfs` с `noexec,nosuid,nodev` | во временном каталоге нельзя выполнить файл |
| ротация логов (10 МБ на 3 файла) | переполнение диска логами — реальный сценарий отказа на малом VPS |
| непривилегированный образ nginx | мастер-процесс не запускается от root |
| код принадлежит root, приложению доступен только на чтение | процесс не может переписать собственные исходники |

### nginx

| Мера | Назначение |
|---|---|
| catch-all сервер с `return 444` | запрос с неизвестным `Host` обрывается без ответа |
| ограничение методов | всё, кроме `GET/HEAD/POST`, отбрасывается до Python |
| `limit_req` и `limit_conn` | защита от перебора и исчерпания соединений |
| таймауты заголовков и тела | защита от Slowloris |
| `client_max_body_size` | защита от раздувания тела запроса |
| белый список расширений в `/media/` | SVG исключён: это XML, он может содержать скрипт, и с нашего origin это хранимая XSS |
| блокировка точечных файлов и резервных копий | `.git`, `.env` и подобное недоступны |
| `server_tokens off` | версия не подсказывает, какие CVE пробовать |
| `X-Forwarded-For` равен `$remote_addr` | не `$proxy_add_x_forwarded_for`: иначе клиент подделал бы источник запроса и обошёл любую логику на основе IP |
| `absolute_redirect off` | внутренний порт 8080 не утекает в заголовок `Location` |
| `gzip_proxied` без ответов с cookie | защита от BREACH |

### Django

| Мера | Назначение |
|---|---|
| отказ старта при слабом `SECRET_KEY` | исключает тихий выезд в продакшен с ключом из шаблона |
| `ALLOWED_HOSTS` из конфига | защита от подмены `Host` |
| `USE_X_FORWARDED_HOST = False` | защита от host header injection в ссылках писем |
| Argon2id для паролей | рекомендация OWASP; PBKDF2 по умолчанию на порядок дешевле перебирать на GPU |
| минимальная длина пароля 10 | против словарного перебора |
| `HttpOnly` на обеих cookie | токен и сессия недоступны из JavaScript |
| `SameSite=Lax` | дополнение к CSRF-токену |
| строгая CSP без `unsafe-inline` | внешний скрипт не выполнится, даже попав в HTML через XSS |
| лимиты на число полей и размер формы | защита от DoS через разбор гигантских форм |
| отсутствие подсистем без БД | неработающий код не загружается вовсе |
| админка выключена и запрещена без TLS | при выключенном TLS пароль администратора шёл бы открытым текстом |

---

## 4. Принятые риски

### Риск 1: трафик не шифруется

**Статус:** принят на этапе 0, обязателен к устранению до этапа 1.

Сейчас `[tls].enabled = false`. Это допустимо ровно потому, что сайт не
передаёт ни паролей, ни сессионных cookie: аутентификации ещё нет.

С появлением формы входа риск становится неприемлемым: логин, пароль и
сессионную cookie будет видеть любой узел на маршруте. Домен для решения
не нужен:

```
make tls-selfsigned
# в config/selectashop.toml установить [tls].enabled = true
make config && make restart
```

Браузер покажет предупреждение — доверенного центра сертификации за
самоподписанным сертификатом нет, — но трафик будет зашифрован. Когда
появится домен, следует перейти на Let's Encrypt.

Защита устроена так, что забыть об этом нельзя: включение админки при
выключенном TLS блокируется на уровне генерации конфигурации.

### Риск 2: без домена невозможен доверенный сертификат

Центры сертификации не выдают публично доверенные сертификаты на
IP-адрес по обычной процедуре. Пока домена нет, вариантов два:
самоподписанный сертификат с предупреждением браузера либо открытый
трафик. Первый строго лучше второго.

### Риск 3: зависимости закреплены по версии, а не по хешу

`requirements.txt` фиксирует точные версии, но не контрольные суммы.
Это защищает от неожиданного обновления, но не от компрометации самого
индекса пакетов. Переход на `pip install --require-hashes` уместен,
когда состав зависимостей стабилизируется.

### Риск 4: nginx кеширует адрес приложения

nginx резолвит имя `web` один раз при старте. Если пересоздать только
контейнер приложения, он получит новый IP, а nginx продолжит обращаться
по старому. Поэтому `make restart` пересоздаёт оба сервиса вместе.
Отдельный `docker compose restart web` использовать не следует.

---

## 5. Не входит в этап 0

Появится на последующих этапах:

- **Этап 1:** TLS, аутентификация, строгий rate-limit на формы входа
  (зона `auth` уже описана в конфигурации), защита от перебора,
  журнал событий безопасности
- **Этап 2:** проверка загружаемых изображений по содержимому, а не по
  расширению
- **Этап 3:** шифрование ключей товаров в БД, выдача через
  `SELECT ... FOR UPDATE SKIP LOCKED`
- **Этап 4:** проверка подписи вебхуков, идемпотентность платежей
- **Этап 6:** резервное копирование БД, мониторинг, fail2ban или
  аналог на уровне хоста

---

## 6. Меры за пределами проекта

Настраиваются на самом VPS и в состав репозитория не входят:

1. **Файрвол.** Открыть только 22, 80 и 443. Docker вносит правила в
   `iptables` в обход `ufw` — проверять следует через `iptables -L -n`,
   а не через `ufw status`.
2. **SSH.** Запретить вход по паролю и вход для root, оставить только
   ключи.
3. **Swap.** Файл подкачки на 2 ГБ. На 1 ГБ RAM это разница между
   «медленно» и «сервер умер».
4. **Автоматические обновления безопасности** пакетов хоста.
5. **Регулярное обновление образов** и повторный прогон `make audit`.
