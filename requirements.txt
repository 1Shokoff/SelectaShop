# Версии зафиксированы точно. Обновление — осознанное действие с проверкой
# changelog и прогоном `make audit`, а не следствие пересборки образа.
#
# Драйвер PostgreSQL (psycopg) намеренно отсутствует: на этапе 0 базы нет,
# а каждый лишний пакет в образе — это дополнительный источник CVE.
# Он появится здесь вместе с контейнером БД на этапе 1.

Django==5.2.17
gunicorn==26.2.0

# Argon2id для хеширования паролей. Нужен уже сейчас, потому что на него
# ссылается PASSWORD_HASHERS в settings.py.
argon2-cffi==25.1.0
