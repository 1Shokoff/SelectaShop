# Версии зафиксированы точно. Обновление — осознанное действие с проверкой
# changelog и прогоном `make audit`, а не следствие пересборки образа.

Django==5.2.17
gunicorn==26.2.0

# Argon2id для хеширования паролей — рекомендация OWASP.
# PBKDF2, применяемый Django по умолчанию, на порядок дешевле перебирать
# на видеокартах.
argon2-cffi==25.1.0

# Драйвер PostgreSQL. Вариант binary включает собранный libpq, поэтому
# в образ не нужно тянуть postgresql-client и заголовочные файлы.
psycopg[binary]==3.2.13

# AES-256-GCM для шифрования персональных данных и HMAC для слепых
# индексов. Хранить адреса почты открытым текстом мы не хотим: украденный
# дамп не должен быть готовой базой контактов.
cryptography==50.0.1
